// 안녕하세요, 두프룸프님!
// 이 파일은 자바의 '연산자(Operator)' 중에서도 특별히 '삼항 연산자(Ternary Operator)'에 대해 배우는 예제입니다.
// 연산자는 프로그래밍에서 데이터를 계산하거나, 비교하거나, 조작할 때 사용하는 특별한 기호들을 말해요.
// 삼항 연산자는 조건에 따라 두 가지 값 중 하나를 선택할 때 아주 유용하게 쓰입니다.

public class _03_Operator { // 클래스 선언
    public static void main(String[] args) { // main 메서드: 프로그램 시작점

        // [1] 기본적인 삼항 연산자: 두 가지 경우(크다/작다) 판별
        // 삼항 연산자의 기본 구조는 다음과 같아요:
        // (조건식) ? (조건식이 참일 때의 값) : (조건식이 거짓일 때의 값);
        // 물음표(?) 앞의 조건식이 true이면 콜론(:) 앞의 값을 선택하고,
        // false이면 콜론(:) 뒤의 값을 선택합니다.

        int a = 11; // 정수형 변수 a에 11 할당
        int b = 11; // 정수형 변수 b에 11 할당

        // 예시: a가 b보다 큰가? (a > b)
        // 조건식: a > b (11 > 11) -> 결과는 false
        // 따라서 콜론(:) 뒤의 값인 "a가 작다!"가 선택되어 result 변수에 저장됩니다.
        String result = a > b ? "a가 크다!" : "a가 작다!";
        System.out.println("단순 비교 결과: " + result); // "단순 비교 결과: a가 작다!" 출력

        System.out.println("\n----------------------------------------\n"); // 구분선 출력

        // [2] 중첩된 삼항 연산자: 세 가지 경우(크다/작다/같다) 판별
        // 삼항 연산자의 '거짓일 때의 값' 부분에 또 다른 삼항 연산자를 넣어서
        // 마치 if-else if-else 문처럼 여러 조건을 처리할 수 있습니다.

        // 현재 a=11, b=11 입니다.
        // result = (a >= b) ? (a == b ? "a와 b는 같다." : "a는 b보다 크다.") : "a는 b보다 작다.";

        // 이 코드를 단계별로 분석해볼까요?
        // 1. 가장 바깥쪽 조건식 평가: (a >= b)
        //    (11 >= 11) -> 결과는 true 입니다.
        //    따라서 물음표(?) 바로 뒤의 값인 (a == b ? "a와 b는 같다." : "a는 b보다 크다.") 이 부분이 실행됩니다.

        // 2. 안쪽 삼항 연산자 평가: (a == b ? "a와 b는 같다." : "a는 b보다 크다.")
        //    조건식: (a == b) (11 == 11) -> 결과는 true 입니다.
        //    따라서 물음표(?) 바로 뒤의 값인 "a와 b는 같다."가 최종적으로 선택됩니다.
        result = (a >= b) ? (a == b ? "a와 b는 같다." : "a는 b보다 크다.") : "a는 b보다 작다!";
        System.out.println("중첩 비교 결과: " + result); // "중첩 비교 결과: a와 b는 같다." 출력

        /*
        // 두프룸프님, 위에서 본 중첩 삼항 연산자는 사실 아래의 if-else if-else 문과 완전히 동일한 로직이에요.
        // if-else if-else 문이 더 익숙하고, 복잡한 조건에서는 가독성이 더 좋을 수 있습니다.
        // 삼항 연산자는 코드를 한 줄로 간결하게 표현할 수 있다는 장점이 있지만,
        // 너무 복잡하게 중첩하면 오히려 코드를 이해하기 어려워질 수 있으니 적절히 사용하는 것이 중요해요!
        if (a >= b) { // 만약 a가 b보다 크거나 같다면...
            if (a == b) { // 그 중에서 a와 b가 같다면...
                System.out.println("a와 b는 같다.");
            } else { // a가 b보다 크고, 같지는 않다면 (즉, a > b)
                System.out.println("a는 b보다 크다.");
            }
        } else { // a가 b보다 작다면 (a < b)
            System.out.println("a는 b보다 작다.");
        }
        */
    }
}
