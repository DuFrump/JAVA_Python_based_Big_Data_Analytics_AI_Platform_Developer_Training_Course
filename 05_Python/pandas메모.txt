### 데이터 프레임 만들기
df2 = pdf2 = pd.DataFrame([[15, '남', '덕영중'], [17, '여', '수리중']], 
			index=['준서', '예은'], 
			columns=['나이', '성별', '학교'])

##########################################################

### 딕셔너리로 데이터 프레임 만들기
dict_data = {'c0' : [1, 2, 3], 'c1' : [4, 5, 6], 'c2' : [7, 8, 9]}
df = pd.DataFrame(dict_data)

##########################################################

### <index, 컬럼 이름 설정> -- 전체 바꿈
df2.index=['학생1','학생2']
df2.column=['연령','남녀','소속']

### <rename> -- 지정해서 바꿈
df2.rename(index={'학생1':'김학생'}, inplace=True)
df2.rename(columns={'연령':'age', '남녀':'gender', '소속':'school'})

### <set.index> -- 인덱스 바꿈
ndf = df.set_index('이름') --- 기존 인덱스 사라짐

### <reindex> -- 인덱스 바꿈
ndf = df.reindex(columns=['이름', '영어'])
--- 기존 개수, 종류 상관 없이 '이름', '영어'로 재생성 (자리 바꾸기 가능)
--- 없던 컬럼이라면 NaN 값으로 채워짐
--- fill_value=80 --- 옵션 값으로 채워짐

### <reset_index> -- 인덱스 초기화
ndf = df.reset_index() -- 기존 인덱스를 칼럼으로 이동시키고, 1, 2, 3... 기본 숫자가 인덱스 자리를 대신 함.
ndf = df.reset_index(names=['id']) --- 기존 인덱스를 칼럼으로 이동 시키면서 칼럼 이름 추가
ndf = df.reset_index(drop=True) --- 기존 인덱스를 삭제

### <sort_index/values> -- 인덱스/특정컬럼 기준으로 정렬
ndf = df.sort_index(ascending=False) -- 인덱스 순서로 내림 정렬
ndf = df.sort_values(by='수학', ascending=False) -- '수학' 컬럼 기준으로 내림 정렬
ndf = ndf.sort_values(by=['수학', '영어'], ascending=False) -- '수학' 컬럼 기준으로 내림 정렬 이후 '영어' 기준으로 내림 재정렬

##########################################################

### <drop> -- 삭제
df1 = df.drop('철수', axis=0)
df2 = df.drop('수학	', axis=1)
df.drop('수학', axis=1, inplace=True)
df3 = df.drop(['수학', '영어'], axis=1)

### 행 선택
df.loc['철수']
df.iloc[0]

df.loc[['철수','영희']]
df.iloc[0:3]

df.loc['철수':'영희']
df.iloc[[0, 1]]

### 열 선택
df['수학']
df[['수학':'영어']]

### 행, 열 선택
df.iloc[0:5] --- 0 ~ 4 행 선택
df.iloc[0:10:2] --- 0 ~ 9 행 사이 두 칸씩 건너 선택
df.iloc[0:5, 3:8] --- 0 ~ 9 행 그리고 3 ~ 7 열 선택
df.iloc[:, 5:10] --- 모든 행 그리고 5 ~ 9 열 선택

### <다양한 선택>
df.loc['철수','수학']  df2.iloc[0,0] --- 원소 반환
df.loc['철수',['수학','영어']]  df2.iloc[0,[0,1]] --- 시리즈 반환
df.loc[['철수','정수'],['수학','영어']]  df2.iloc[[0,2],[0,1]] --- 데이터 반환
df.loc['철수':'정수','수학':'영어']  df2.iloc[0:2,0:1] --- 데이터 반환

##########################################################

### <열 추가>
df['국어'] = 80 --- 열 선택과 같음. 데이터 80으로 통일
df['미술'] = [89, 91, 91] --- 데이터 다 넣으려면 개수 맞추기

### <행 추가>
df.loc['동수'] = 0 --- 행 선택과 같음. 데이터 0으로 통일
df.loc['말숙'] = [34, 54, 67, 88, 96] --- 데이터 다 넣으려면 개수 맞추기

###########################################################

### <원소 값 변경>
df.loc['동수', '수학'] = 65
df.iloc[3, 1] = 70
df.loc['동수', '음악':'미술'] = [45, 78, 94]
--- 범위 선택해서 데이터 넣어주면 OK

### <전치>
df = df.transpose()
df = df.T

###########################################################

titanic = sns.load_dataset('titanic')
print(df.head()) --- 상위 5개 뽑기
print(df.tail()) --- 하위 5개 뽑기
df.info() --- 데이터 프레임 정보 보기
